# Cities - Города России

React + Vite приложение для просмотра списка городов России с фильтрацией и сортировкой.

## Возможности

- 📋 Просмотр списка городов с информацией о населении, регионе и федеральном округе
- 🔍 Поиск по названию города, региону или федеральному округу
- 🎯 Фильтрация по:
  - Федеральным округам (с чекбоксами)
  - Населению (диапазон от/до)
- 📊 Сортировка по:
  - Названию города (алфавитная)
  - Населению (возрастание/убывание)
- 📱 Адаптивный дизайн

## Установка

```bash
npm install
```

## Запуск

```bash
# Режим разработки
npm run dev

# Сборка для продакшена
npm run build

# Просмотр production сборки
npm run preview
```

## Деплой в GitHub Pages

### Шаг 1: Включите GitHub Pages в настройках репозитория

1. Перейдите в **Settings** вашего репозитория на GitHub
2. В левом меню найдите раздел **Pages**
3. В разделе **Source** выберите **GitHub Actions**
4. Сохраните изменения

### Шаг 2: Настройте base path (если нужно)

Если ваш репозиторий **НЕ** является root (не `username.github.io`), обновите `vite.config.js`:

```js
base: '/имя-вашего-репозитория/',
```

Например, если репозиторий называется `cities`:
```js
base: '/cities/',
```

### Шаг 3: Запушьте код

```bash
git add .
git commit -m "Add GitHub Pages deployment"
git push origin main
```

### Шаг 4: Проверьте деплой

1. Перейдите на вкладку **Actions** в репозитории
2. Дождитесь завершения workflow "Deploy to GitHub Pages"
3. После успешного деплоя приложение будет доступно по адресу:
   - `https://username.github.io/cities/` (если настроен base path)
   - `https://username.github.io/` (если репозиторий root)

**Примечание:** Первый деплой может занять несколько минут. Последующие деплои будут происходить автоматически при каждом пуше в ветку `main` или `master`.

## Технологии

- React 18
- Vite 5
- JavaScript (ES6+)

## Данные

Данные о городах взяты из открытого источника: [russia-cities](https://github.com/arbaev/russia-cities)
